<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.input :ingredient_id, collection: Ingredient.all %>
  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>

<!-- TO-DO: Selecionar cocktails existentes. -->
<!-- TO-DO: Selecionar ingredientes existentes. -->
